apiVersion: v1
kind: Secret
metadata:
  name: lcagent-secrets
  namespace: lcagentns-app
type: Opaque
stringData:
  # 敏感信息应该通过环境变量或外部密钥管理系统注入
  # 这里仅作为示例，生产环境请使用更安全的方式
  SMTP_USERNAME: ""
  SMTP_PASSWORD: ""
  MCP_EMAIL_PASSWORD: ""
  MCP_EMAIL_USER: ""
  REDIS_PASSWORD: "lazyai123456"
  MINIO_ACCESS_KEY: "minioadmin"
  MINIO_SECRET_KEY: "minioadmin"

